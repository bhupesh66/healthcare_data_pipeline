{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Current File (debugpy)",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "env": {
        "PATH": "/opt/anaconda3/envs/myenv/bin:${env:PATH}"
      }
    }
  ]
}
